[project]
name = "couimet-github-io"
version = "0.1.0"
requires-python = ">=3.10"

[dependency-groups]
dev = [
    "ruff==0.16.0",
    "python-docx>=1.1.0",
    "pytest>=9.1.1",
    "pillow>=12.3.0",
    "pyyaml>=6.0.3",
    "jsonschema[format]>=4.26.0",
]

[tool.ruff.lint]
allowed-confusables = ["–"]
