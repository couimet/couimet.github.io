[project]
name = "couimet-github-io"
version = "0.1.0"
requires-python = ">=3.10"

[dependency-groups]
dev = [
    "ruff==0.15.17",
    "python-docx>=1.1.0",
    "pytest>=9.1.1",
    "pillow>=12.3.0",
    "pyyaml>=6.0.3",
]

[tool.ruff.lint]
allowed-confusables = ["–"]
