# Shared layout constants so card sections stay aligned when one changes.
# Card templates read `card_grid`; index.html reads the `homepage` follow_along limit.

card_grid: "col-md-6 col-lg-3"

homepage:
  follow_along: 4
